要求：
    利用rabbitMQ实现
    1. 可以执行多个命令
    2. 对多台机器
    3. 命令格式
        >>:run "df -h" --host 192.168.17.136 ***
        task id:45334
        >>:checktask 45334
        >>:

思路：
    服务端：在窗口每输入一条命令，都另起一个进程，使其成为一个守护进程，在这个守护进程中
    通过协程的方式处理语句，之后返回
    客户端：执行完命令之后，将消息通过rabbitmq发送到服务端rabbitmq队列